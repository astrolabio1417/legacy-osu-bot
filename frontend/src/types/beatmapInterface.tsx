export interface IBeatmap {
  ar: [number, number];
  bpm: [number, number];
  cs: [number, number];
  length: [number, number];
  star: [number, number];
  rank_status: string[];
  genre: string;
  language: string;
}
