export interface IBeatmap {
  ar: [number, number];
  bpm: [number, number];
  cs: [number, number];
  current: number;
  current_set: number;
  length: [number, number];
  od: [number, number];
  star: [number, number];
  rank_status: string[];
}
